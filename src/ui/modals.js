/*
 * → Developed by: @Mateuszvip60
 * → Obsługuje pokazywanie modalnych okien gry.
 */

export function showModal(element) {
    element.classList.add("show");
}

export function hideModal(element) {
    element.classList.remove("show");
}
